package main

const (
	DNSLookup        string = "DNS lookup"
	TCPConnection    string = "TCP connection"
	TLSHandshake     string = "TLS handshake"
	serverProcessing string = "Server processing"
	contentTransfer  string = "Content transfer"
	connect          string = "Connect"
	nameLookup       string = "Name Lookup"
	preTransfer      string = "Pre Transfer"
	startTransfer    string = "Start Transfer"
	total            string = "Total"
)
